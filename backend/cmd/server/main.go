package main

import "log"

// Phase 1 scaffold stub. The real MCP server (pgx pool + tools + stdio transport)
// is wired up in Phase 4.
func main() {
	log.SetFlags(0)
	log.Println("GoLink MCP server scaffold - OK")
}
